# Only needed once your backend is deployed (e.g. to Render).
# Locally, api.js already defaults to http://localhost:5000/api/workouts,
# so you don't need this file for local development.

VITE_API_URL=https://your-backend-name.onrender.com/api/workouts
